# Elice Animation Replacer (EAR v1.0)

이 프로젝트는 Elice (elice.io) 플랫폼의 학습 환경을 개선하기 위한 Chrome 확장 프로그램입니다.

코드 제출 시 기본으로 출력되는 Lottie 기반의 정답/오답 애니메이션을 사용자가 지정한 커스텀 이미지와 애니메이션으로 대체하여, 더 나은 경험을 선사합니다.

---

## ✨ 주요 기능

* **정답/오답 애니메이션 교체:** 정답(토끼) 및 오답(거북이) 애니메이션을 개별적으로 교체합니다.
* **커스텀 이미지 지원:** 팝업 메뉴를 통해 이미지 URL 또는 로컬 파일을 업로드하여 사용할 수 있습니다.
* **부드러운 애니메이션:** CSS `@keyframes`를 사용한 부드러운 등장/퇴장 애니메이션을 제공합니다. (현재 화면 `70vmin` 크기)
* **ON/OFF 토글:** 확장 프로그램의 전체 기능을 팝업에서 쉽게 켜고 끌 수 있습니다.
* **정답 피드백 강화:** 정답(토끼)의 경우, 플랫폼의 기본 설정보다 더 길게 표시하여 정답의 기쁨을 더 누리실 수 있습니다.
* **기본값 복구:** '설정 초기화' 시, 사용자가 지정한 기본 URL로 설정을 복구합니다.

---

## 🛠️ 아무도 궁금해하지 않을 기술적 원리

1.  **DOM 감지:** `MutationObserver`를 사용하여 Elice 페이지의 DOM 변경을 감시합니다.
2.  **신호 포착:** 정답/오답 컨테이너(`[class*="StyledRabbitAnimation"]`)가 DOM에 추가되는 것을 감지하고, 해당 컨테이너의 자식 요소(정답/오답 div)에 두 번째 `MutationObserver`를 연결합니다.
3.  **이벤트 트리거:** 두 번째 `MutationObserver`는 `u-no-display` 클래스 속성의 변경을 감지하여 애니메이션의 '표시' 및 '숨김' 이벤트를 트리거합니다.
4.  **독립 컨테이너:** Elice의 CSS(`display: none`)에 영향을 받지 않기 위해, `document.body`에 직접 `eac-container`라는 독립적인 div를 생성합니다.
5.  **원본 숨김:** '표시' 신호가 감지되면, **원본 Lottie div의 `opacity`를 0으로** 설정하여 소리는 재생되되 시각적으로는 보이지 않게 처리합니다.
6.  **이미지 주입:** `eac-container`에 사용자가 설정한 이미지를 주입하고 CSS 애니메이션을 재생합니다.
7.  **지연 및 복구:** '숨김' 신호가 감지되면, 오답(거북이)은 즉시 사라지며, 정답(토끼)은 설정된 지연 시간(3초) 후에 사라집니다. 애니메이션이 종료되면 원본 div의 `opacity`를 1로 복구합니다.

---

## ⚠️ 면책 조항 (및 Elice에 드리는 말씀)

이 확장 프로그램은 Elice 플랫폼의 학습 효율과 즐거움을 높이고자 하는 **순수한 팬심**과 **학습 목적**으로 개발되었습니다.

(토끼도 좋지만 저는 미소녀를 더 좋아해서 만들었습니다.🐰)

이 프로젝트는 Elice의 서비스에 어떠한 악의적인 영향을 주거나  서버에 부담을 주거나 과제의 부정행위/치팅의 일환으로 작용하지 않는, 전적으로 클라이언트 측(사용자 브라우저)에서만 작동하는 코드입니다.

혹시... 혹시라도 이 프로젝트가 문제가 된다면, (고소장... 대신) 부드럽게 연락 주시면 감사하겠습니다. 🥺

> This project is a fan-made tool developed for educational purposes and is not affiliated with, or endorsed by, Elice.